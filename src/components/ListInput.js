import React from 'react';

function ListInput() {

    return(
        <div>
            <input placeholder="e.g. Titanic"/>
            <button>Add Item</button>
        </div>
    )
}

export default ListInput;